server:
  port: 7777

invoicing-system:
  database: file
  database.directory: db
  database.idFileName: db-ids.json
  database.dataFileName: db-data.json

logging:
  level:
    pl:
      futurecollars:
        invoicing: