server:
  port: 7777

logging:
  level:
    org:
      springframework:
        boot: INFO
    pl:
      futurecollars:
        invoicing: INFO
        invoicing.db: DEBUG

spring:
  profiles:
    active: mongo
  flyway:
    enabled: false
  security:
    user:
      name: pawel
      password: xxx1234

invoicing-system:
  allowedOrigins: http://localhost:4200
  csrf:
    disable: true
