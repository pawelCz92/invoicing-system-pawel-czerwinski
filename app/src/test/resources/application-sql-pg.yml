spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/invoicing-test
    username: postgres
    password: pawel1
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true

invoicing-system:
  database: sql